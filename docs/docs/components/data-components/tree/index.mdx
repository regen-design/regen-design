---
sidebar_position: 6
---

import BrowserOnly from '@docusaurus/BrowserOnly'
import { CodeBlock, Grid, ThemeProvider } from '@regen-design/components'
import Basic from "./Basic"
import BasicCode from "!!raw-loader!./Basic"
import CustomKeyAndLabel from "./CustomKeyAndLabel"
import CustomKeyAndLabelCode from "!!raw-loader!./CustomKeyAndLabel"
import Multiple from "./Multiple"
import MultipleCode from "!!raw-loader!./Multiple"

# 树 Tree

老实说我生物不好，认不出几种树来。

以及，不光是生物不好，平衡树也是看了就忘。

## 演示

<BrowserOnly>
  {() => (<ThemeProvider>
    <Grid xGap={16} cols={2} style={{
      alignItems: 'flex-start'
    }}>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="基础"
                   description={'好在这颗树不是活的，也不平衡。'}
                   code={String(BasicCode)}>
          <Basic />
        </CodeBlock>
        <CodeBlock title="多选节点"
                   description={'设置 `multiple` 来允许多选节点。'}
                   code={String(MultipleCode)}>
          <Multiple />
        </CodeBlock>
      </Grid>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="自定义 key 和 label 的字段"
                   description={'后端会传来各种各样的数据。'}
                   code={String(CustomKeyAndLabelCode)}>
          <CustomKeyAndLabel />
        </CodeBlock>
      </Grid>
    </Grid>
  </ThemeProvider>)}
</BrowserOnly>