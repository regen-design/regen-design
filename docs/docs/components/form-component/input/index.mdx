---
sidebar_position: 5
---

import { CodeBlock, Grid, ThemeProvider } from '@regen-design/components'
import BrowserOnly from '@docusaurus/BrowserOnly'
import Basic from "./Basic"
import BasicCode from '!!raw-loader!./Basic'
import Size from "./Size"
import SizeCode from '!!raw-loader!./Size'
import Rounded from "./Rounded"
import RoundedCode from '!!raw-loader!./Rounded'
import PrefixSuffix from "./PrefixSuffix"
import PrefixSuffixCode from '!!raw-loader!./PrefixSuffix'
import Disabled from "./Disabled"
import DisabledCode from '!!raw-loader!./Disabled'
import Count from "./Count"
import CountCode from '!!raw-loader!./Count'
import Clearable from "./Clearable"
import ClearableCode from '!!raw-loader!./Clearable'
import Password from "./Password"
import PasswordCode from '!!raw-loader!./Password'
import SpecificValue from "./SpecificValue"
import SpecificValueCode from '!!raw-loader!./SpecificValue'

# 文本输入 Input

文本输入框用于用户输入表单数据，支持多种类型、尺寸、状态等。

<BrowserOnly>
  {() => (<ThemeProvider>
    <Grid xGap={16} cols={2} style={{
      alignItems: 'flex-start'
    }}>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="基础"
                   code={String(BasicCode)}>
          <Basic />
        </CodeBlock>
        <CodeBlock title="圆角"
                   code={String(RoundedCode)}>
          <Rounded />
        </CodeBlock>
        <CodeBlock title="禁用"
                   code={String(DisabledCode)}>
          <Disabled />
        </CodeBlock>
        <CodeBlock title="可清除"
                   code={String(ClearableCode)}>
          <Clearable />
        </CodeBlock>
        <CodeBlock title="只允许输入特定值"
                   code={String(SpecificValueCode)}>
          <SpecificValue />
        </CodeBlock>
      </Grid>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="尺寸"
                   code={String(SizeCode)}>
          <Size />
        </CodeBlock>
        <CodeBlock title="密码"
                   code={String(PasswordCode)}>
          <Password />
        </CodeBlock>
        <CodeBlock title="前缀&后缀"
                   code={String(PrefixSuffixCode)}>
          <PrefixSuffix />
        </CodeBlock>
        <CodeBlock title="字素计数"
                   code={String(CountCode)}>
          <Count />
        </CodeBlock>
      </Grid>
    </Grid>
  </ThemeProvider>)}
</BrowserOnly>