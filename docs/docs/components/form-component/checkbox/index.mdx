import { CodeBlock, Grid, ThemeProvider } from '@regen-design/components'
import BrowserOnly from '@docusaurus/BrowserOnly'
import Basic from "./Basic"
import BasicCode from '!!raw-loader!./Basic'
import Disabled from "./Disabled"
import DisabledCode from '!!raw-loader!./Disabled'
import Size from "./Size"
import SizeCode from '!!raw-loader!./Size'
import Group from "./Group"
import GroupCode from '!!raw-loader!./Group'
import PartiallySelected from "./PartiallySelected"
import PartiallySelectedCode from '!!raw-loader!./PartiallySelected'

# 复选框 Checkbox

复选框用于在一组选项中进行多项选择。

<BrowserOnly>
  {() => (<ThemeProvider>
    <Grid xGap={16} cols={2} style={{
      alignItems: 'flex-start'
    }}>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="基础"
                   code={String(BasicCode)}>
          <Basic />
        </CodeBlock>
        <CodeBlock title="复选组"
                   code={String(GroupCode)}>
          <Group />
        </CodeBlock>
        <CodeBlock title="部分选中"
                   code={String(PartiallySelectedCode)}>
          <PartiallySelected />
        </CodeBlock>
      </Grid>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="禁用"
                   code={String(DisabledCode)}>
          <Disabled />
        </CodeBlock>
        <CodeBlock title="尺寸"
                   code={String(SizeCode)}>
          <Size />
        </CodeBlock>
      </Grid>
    </Grid>
  </ThemeProvider>)}
</BrowserOnly>