---
sidebar_position: 1
---

import { CodeBlock, Grid, ThemeProvider } from '@regen-design/components'
import BrowserOnly from '@docusaurus/BrowserOnly'
import Basic from "./Basic"
import BasicCode from '!!raw-loader!./Basic'
import Disabled from "./Disabled"
import DisabledCode from '!!raw-loader!./Disabled'
import Size from "./Size"
import SizeCode from '!!raw-loader!./Size'
import Group from "./Group"
import GroupCode from '!!raw-loader!./Group'
import ButtonGroup from "./ButtonGroup"
import ButtonGroupCode from '!!raw-loader!./ButtonGroup'

# 单选框 Radio

单选框，用于在多个备选项中选中单个状态。

<BrowserOnly>
  {() => (<ThemeProvider>
    <Grid xGap={16} cols={2} style={{
      alignItems: 'flex-start'
    }}>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="基础"
                   code={String(BasicCode)}>
          <Basic />
        </CodeBlock>
        <CodeBlock title="单选组"
                   code={String(GroupCode)}>
          <Group />
        </CodeBlock>
        <CodeBlock title="按钮单选组"
                   code={String(ButtonGroupCode)}>
          <ButtonGroup />
        </CodeBlock>
      </Grid>
      <Grid yGap={16} cols={1}>
        <CodeBlock title="禁用"
                   code={String(DisabledCode)}>
          <Disabled />
        </CodeBlock>
        <CodeBlock title="尺寸"
                   code={String(SizeCode)}>
          <Size />
        </CodeBlock>
      </Grid>
    </Grid>
  </ThemeProvider>)}
</BrowserOnly>