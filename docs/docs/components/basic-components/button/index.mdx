---
sidebar_position: 1
---

import BrowserOnly from '@docusaurus/BrowserOnly'
import Basic from "./Basic"
import BasicCode from '!!raw-loader!./Basic';
import Dashed from "./Dashed"
import DashedCode from '!!raw-loader!./Dashed';
import Size from "./Size"
import SizeCode from '!!raw-loader!./Size';
import Disabled from "./Disabled"
import DisabledCode from '!!raw-loader!./Disabled';
import Text from "./Text"
import TextCode from '!!raw-loader!./Text';
import Block from "./Block"
import BlockCode from '!!raw-loader!./Block';
import Loading from "./Loading"
import LoadingCode from '!!raw-loader!./Loading';
import Shape from "./Shape"
import ShapeCode from '!!raw-loader!./Shape';
import Colors from "./Colors"
import ColorsCode from '!!raw-loader!./Colors';
import { CodeBlock, Grid, Table, ThemeProvider } from '@regen-design/components'
import Api from "./Api"
import { generateDocs } from '@regen-design/utils/docs'
import ButtonProps from '!!raw-loader!@regen-design/types/button/index.d'

# 按钮 Button

按钮用来触发一些操作。

## 演示
<BrowserOnly>
  {() => (
    <ThemeProvider>
      <Grid xGap={16} cols={2} style={{
        alignItems: 'flex-start'
      }}>
        <Grid yGap={16} cols={1}>
          <CodeBlock title="基础"
                     description={"按钮的 type 分别为 `default`、`tertiary`、`primary`、`info`、`success`、`warning` 和 `error`。"}
                     code={String(BasicCode)}>
            <Basic />
          </CodeBlock>

          <CodeBlock title="禁用"
                     description="按钮可以被禁用。"
                     code={String(DisabledCode)}>
            <Disabled />
          </CodeBlock>

          <CodeBlock title="文本"
                     description="像文本一样的按钮。"
                     code={String(TextCode)}>
            <Text />
          </CodeBlock>

          <CodeBlock title="自定义颜色"
                     description="按钮有加载状态。"
                     code={String(ColorsCode)}>
            <Colors />
          </CodeBlock>

          <CodeBlock title="loading"
                     description="按钮有加载状态。"
                     code={String(LoadingCode)}>
            <Loading />
          </CodeBlock>

        </Grid>
        <Grid yGap={16} cols={1}>
          <CodeBlock title="尺寸"
                     description={"有 `tiny`、`small`、`default` 和 `large` 尺寸。"}
                     code={String(SizeCode)}>
            <Size />
          </CodeBlock>

          <CodeBlock title="虚线"
                     description="使用 `dashed` 来使用虚线按钮。"
                     code={String(DashedCode)}>
            <Dashed />
          </CodeBlock>

          <CodeBlock title="块级"
                     description="`block`属性将使按钮适合其父宽度。"
                     code={String(BlockCode)}>
            <Block />
          </CodeBlock>

          <CodeBlock title="形状"
                     description="按钮拥有不同的形状。"
                     code={String(ShapeCode)}>
            <Shape />
          </CodeBlock>
        </Grid>
      </Grid>
    </ThemeProvider>
  )}
</BrowserOnly>


## API

<>
  <Api data={generateDocs(ButtonProps)}/>
</>