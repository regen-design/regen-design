import { NAME_SPACE } from '@regen-design/constant'
import { CheckboxGroupProps } from '@regen-design/types'
import styled from 'styled-components'
const prefix = `${NAME_SPACE}-checkboxGroup`
export const StyledCheckboxGroupPrefixClass = prefix
export const StyledCheckboxGroup = styled.div<CheckboxGroupProps>``
